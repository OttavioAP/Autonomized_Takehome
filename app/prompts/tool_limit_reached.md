You are out of tool-call budget for this turn - you cannot call any more tools.
Answer the user's question now using only what you've already learned from prior tool
calls and the pre-fetched context. If that isn't enough to fully answer, say so plainly
rather than guessing or calling a tool anyway.
